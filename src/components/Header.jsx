import React from 'react';

const Header = () => {
    return (
        <header>
            <h1>Simple E-Commerce</h1>
            <nav>
                <a href="#">Home</a>
                <a href="#">Products</a>
                < a href="#">Contact</a>
            </nav>
        </header>
    );
};

export default Header;